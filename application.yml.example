server.port: 8080
oauth2:
    discord.client:
        id: "<client id goes here>"
        secret: "<secret client key here>"
        callbackUrl: "https://ssfs.diekautz.dev/api/discord/callback"
    github.client:
        id: "<client id goes here>"
        secret: "<secret client key here>"
        callbackUrl: "https://ssfs.diekautz.dev/api/discord/callback"
stellar:
    federation.domain: "diekautz.dev"
    toml:
        general:
            NETWORK_PASSPHRASE: "Public Global Stellar Network ; September 2015"
            FEDERATION_SERVER: "https://ssfs.diekautz.dev/federation/"

### DATABASE ###
# spring.datasource.url: "jdbc:mysql://${MYSQL_HOST:localhost}:3306/fed_address" # or your custom database adapter
# spring.datasource.username: "fed_admin"
# spring.datasource.password: "secret"